<div class="accordion accordion-flush" id="accordionFlushedit">

    {% for category in list_items %}
    <div class="accordion-item">
        <h2 class="accordion-header">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="subtasks{{category.0.id}}" aria-expanded="false" aria-controls="subtasks{{ category.0.id }}">
                {{ category.0 }}
            </button>
        </h2>
        <div id="subtasks{{ category.0.id }}" class="accordion-collapse collapse" aria-labelledby="flush-headingOne" data-bs-parent="#accordionFlushedit">

            <div class="accordion-body" style="background-color: #ececec; margin:5px 0px 5px 0px;">
                {% for subtask in category.1 %}
                <p>{{ subtask }}</p>
                {% endfor %}
                <p>Add Subtask</p>

                <form action="#" method="post" enctype=multipart/form-data>
                    {% csrf_token %}
                    {{categories_form.as_p}}
                    <input type="hidden" name="category_id" value="{{ category.0.id }}">
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-primary">Create SubTast</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    {% endfor %}
</div>